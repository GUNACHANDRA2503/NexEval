# Optional. Leave unset for local dev (Vite proxies /api → backend :8000).
# Production static build: set to your public API root, including /api, e.g.:
# VITE_API_BASE=https://nexeval-api.onrender.com/api

# VITE_API_BASE=
